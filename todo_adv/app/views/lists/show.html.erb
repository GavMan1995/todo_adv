<h1>Things to do</h1>
<% if @list.items.any? %>
  <ul>
  <% @items.each do |item| %>
  <li>item.item</li>
  <% end %>
  </ul>
<% else %>
  <h3>you have nothing to do, Congrats:)</h3>
<% end %>

<%= link_to 'New', new_item_path %>
